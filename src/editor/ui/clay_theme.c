/**
 * @file clay_theme.c
 * @brief Theme constants (compile-time only, no runtime state).
 *
 * This file exists to satisfy the module convention (one .c per .h).
 * All theme values are #defines in the header. If runtime theme
 * switching is needed later, theme state can live here.
 */

#include "ferrum/editor/ui/clay_theme.h"

/* All theme values are compile-time constants in the header.
 * This translation unit is intentionally minimal. */
