---
id: rpg-8302
status: in_progress
deps: [rpg-i3wx, rpg-nbp2]
links: []
created: 2026-07-19T07:44:03Z
type: task
priority: 2
assignee: KMD
parent: rpg-hjck
tags: [client, renderer, gi]
---
# Wire real renderer + GI into the client; rename demo_client->client

Replace the client's inline forward shader with the render-world module (T6) driven by the streamed scene, so the networked client renders the level with clustered forward+ PBR, baked lightmaps, and dynamic probe GI. Rename demo_client -> client and keep its orchestrator expandable.

## Design

Hook the render-world build after asset streaming completes (demo_client Stage 7 render + the procgen-load block are the insertion points). Networked bodies (BODY_SPAWN/MESH_DATA) become dynamic renderables appended after render_scene_mark_dynamic; their transforms come from the prediction/interp pools each frame. Client already links liball.a (full renderer). Keep --record video capture. Rename target + source (update Makefile line ~1976).

## Acceptance Criteria

Client loads a scene descriptor, streams assets by priority, and renders it with the real forward+/GI path; networked bodies appear as dynamic renderables lit by the scene GI; runs at interactive fps; binary renamed to build/client.


## Notes

**2026-07-19T18:30:17Z**

Inherited from rpg-nbp2/rpg-i3wx: when wiring the client, feed the streamer's loaded assets into the render_world module AND rewire the two GL chunk streamers through the streaming manager -- retire the demo-local sh_stream (its GL_TEXTURE_2D_ARRAY upload becomes a load/upload callback) and make gi_sdf_stream's 3D-texture upload a callback, registering both via fr_chunk_table over an fr_asset_stream_t. The headless residency model + chunk_table + probe gating already exist (rpg-nbp2).

**2026-07-19T19:23:24Z**

Also wire the STREAM_PRIORITY protocol (rpg-3ldk, complete + tested): server computes per-client priorities (server_stream_priority_build) from the player position over the level chunks and sends NET_REPL_SCHEMA_STREAM_PRIORITY on join + as the player moves; the client decodes and calls net_repl_stream_priority_apply on its fr_asset_stream (instantiated here).

**2026-07-19T19:27:52Z**

SCOPE (user): the client loads assets FROM DISK -- including the base scene descriptor, which contains BOTH static and the level's own dynamic bodies -- streams them, and builds the render_world. Server-spawned bodies (BODY_SPAWN) arrive in realtime and are appended as additional dynamic renderables. (Server sending the base level over TCP as a file is a SEPARATE later subsystem -- out of scope here.) So: client-side descriptor load + asset stream + render_world assembly (descriptor-driven, the reusable counterpart of hall_lit_dynamic's inline loading) + networked bodies as dynamics + STREAM_PRIORITY apply + collider->SDF injection; rename demo_client->client.

**2026-07-19T19:28:54Z**

EXECUTION PLAN:
1. client_scene module (descriptor-driven asset+render_world assembly, the reusable counterpart of hall_lit_dynamic.c's inline loading): given scene_desc_t, stream/load each object's mesh (fvma/dmesh) + materials + the 9-layer lightmap SH arrays (lift load_sh_arrays) + sdf prefix + static irradiance volume + probe grid (probe_place_grid from the descriptor spec), then render_world_init (rpg-i3wx).
2. Networked bodies -> dynamics: after render_scene_mark_dynamic, append BODY_SPAWN bodies + the descriptor's own dynamic objects as dynamic renderables; feed transforms from the prediction/interp pools each frame; build posed gi_collider proxies (gi_collider_pose_build, rpg-85as) and inject into the SDF via render_world_update boxes.
3. STREAM_PRIORITY: decode server hints -> net_repl_stream_priority_apply on the client fr_asset_stream.
4. GL streamer rewiring (inherited from nbp2/i3wx): retire demo-local sh_stream, route gi_sdf_stream uploads through fr_asset_stream + fr_chunk_table.
5. Rename demo_client->client (+ demo_server->server in ksqw); keep --record video capture; Makefile target rename.
6. Verify live: FAISS_STUB=1 build; run 'server --level great_hall.scene' + 'client'; screenshot the networked lit hall at interactive fps.
Verification note: unlike 51nf..3ldk (unit-tested), 8302's render correctness needs a live server<->client run + screenshot.

**2026-07-19T19:30:51Z**

PREREQUISITE FOUND (step 1 blocker): the scene descriptor's materials are NAMES only (scene_desc.materials[N][64]); render_world/PBR needs full material DEFINITIONS (albedo/normal/metallic/roughness/ao/emissive texture paths + metalness, roughness_min/max, normal_scale, uv_scale, tint, contrast, emissive) which hall_lit_dynamic.c currently HARDCODES per great_hall material. So step 1 (client_scene) needs a material-definition schema first: extend scene_desc materials to a scene_desc_material_t (name + tex paths + PBR params), parse string-or-object entries (string => name+defaults), update great_hall.scene with full defs matching the demo, and a texture-streaming path. This is a discrete headless+testable sub-step (like rpg-51nf/b5r3) that unblocks the GL client_scene assembly. Suggest doing it as the first commit of 8302.

**2026-07-19T19:34:13Z**

STEP 1 DONE (committed): material-definition schema. scene_desc materials went from names[N][64] to scene_desc_material_t[N] (name + 6 texture paths albedo/normal/metallic/roughness/ao/emissive + tint/metalness/roughness_min-max/normal_scale/uv_scale/contrast/ao_strength/emissive/orm_packed). Parser accepts a bare string (name + engine defaults) OR a full PBR object; material name->index unchanged. scene_desc_tests 16/16 (added material_definition; string + object forms). Unblocks the GL client_scene material loading. REMAINING for 8302: GL client_scene assembly (mesh/texture streaming + lightmap + render_world), networked bodies as dynamics, STREAM_PRIORITY wire, GL streamer rewire, rename, live verify.

**2026-07-19T19:44:42Z**

STEP 2 (client_scene module) landed: src/renderer/world/client_scene_{load,lightmap}.c + client_scene.h. Descriptor-driven: loads each object's .fvma mesh + material textures (via a caller image-decode callback so the renderer lib stays codec-free) from base_dir, the baked lightmap SH arrays (single-atlas .flm, lifted from load_sh_arrays), computes the scene AABB from mesh bounds, places the probe grid (probe_place_grid from the descriptor spec), and assembles a render_world (forward+ PBR + baked lightmap + dynamic probe GI, GI colliders cap 64 for injection). client_scene_render = render_world_update. Compiles into liball; hall_lit_dynamic unaffected (RENDER_WORLD_SRC pinned to the render_world files). v1 uses ambient+baked lightmap (dynamic level lights = follow-on schema). NEXT: wire into demo_client (load level + render static via client_scene, networked bodies as dynamics) + rename + live verify.
